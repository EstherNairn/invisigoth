[project]
name = "mymodule"
version = "0.1.0"
description = "Python macOS template with submodules"
authors = [{ name = "You" }]
dependencies = ["rich"]

[project.optional-dependencies]
dev = ["pytest"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
